<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aminbadh.tdrcontentcreatorlpm">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TDRContentCreator">
        <activity android:name=".activity.PasswordChangeActivity" />
        <activity android:name=".activity.EmailChangeActivity" />
        <activity
            android:name=".activity.AccountCreatorAdminActivity"
            android:parentActivityName=".activity.SelectionActivity" />
        <activity
            android:name=".activity.AccountCreatorProfessorActivity"
            android:parentActivityName=".activity.SelectionActivity" />
        <activity
            android:name=".activity.SelectionActivity"
            android:launchMode="singleTop" />
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>